# 安全策略

## 报告安全问题

请不要在公开 Issue 中披露密钥、可利用细节或用户数据。仓库维护者应通过 GitHub 的 **Security → Report a vulnerability** 私密安全公告接收报告。

报告中请包含：

- 受影响的接口、文件或版本；
- 可重复的最小步骤；
- 实际影响与建议修复方式；
- 已采取的避免真实用户或外部服务受影响的措施。

## 支持范围

当前仅维护 `main` 分支。默认离线模式适合本地开发和演示；公网部署方还必须自行配置 TLS、持久化存储、网关请求大小限制、供应商配额、内容审核和监控告警。
